version: 3
workflows:
  custom:
    plan:
      steps:
        - run: 
            name: Custom Terraform Init
            command: terraform init -migrate-state -input=false
        - plan
    apply:
      steps:
        - apply
projects:
  - dir: environment/test
    workflow: custom
