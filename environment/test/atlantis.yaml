version: 3
workflows:
  custom:
    plan:
      steps:
        - init:
            extra_args: ["-migrate-state"]
        - plan
    apply:
      steps:
        - apply
projects:
  - name: eks-atlantis-demo
    dir: environment/test
    workflow: custom
