version: 3
projects:
- name: eks-atlantis-demo
  dir: environment/test
  workflow: custom
workflows:
  custom:
    plan:
      steps:
      - init:
          extra_args: ["-migrate-state"]
      - plan
