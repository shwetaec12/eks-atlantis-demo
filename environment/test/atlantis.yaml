version: 3
workflows:
  custom:
    plan:
      steps:
        - init
        - plan:
            extra_args: ["-migrate-state"]
    apply:
      steps:
        - apply
projects:
  - dir: environment/test
    workflow: custom
